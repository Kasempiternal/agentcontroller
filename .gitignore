.build/
build/
.DS_Store
*.xcodeproj/
.swiftpm/
.tmp

# Windows/.NET
bin/
obj/
publish/
*.user
.dotnet-cli/

# Local MCP client registration. The client needs an absolute path, which is
# machine-specific, so this is per-checkout. Copy .mcp.json.example instead.
.mcp.json
